/**
 * Bump this when you deploy so you can confirm GitHub Pages picked up the build.
 * Shown in the header as “Kite Wallah v1.6”.
 */
export const APP_VERSION = "1.45";

/** Optional note — not shown in UI */
export const APP_VERSION_NOTE = "how-it-works decision trees · live sync indicator";
