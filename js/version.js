/**
 * Bump this when you deploy so you can confirm GitHub Pages picked up the build.
 * Shown in the header as “Kite Wallah v2.0”.
 */
export const APP_VERSION = "2.0.6";

/** Optional note — not shown in UI */
export const APP_VERSION_NOTE = "v2 OpenAI layer (gpt-4o-mini) on Plan + Now · rules pick kites";
